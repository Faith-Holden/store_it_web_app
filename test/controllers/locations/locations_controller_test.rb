require 'test_helper'
class Locations::LocationssControllerTest < ActionDispatch::IntegrationTest
  # ----tests for redirecting if wrong user ------------
  test "Should redirect create if wrong user" do
    flunk "test is not yet written"
  end

  test "Should redirect new if wrong user" do
    flunk "test is not yet written"
  end

  test "Should redirect destroy if wrong user" do
    flunk "test is not yet written"
  end

  test "Should redirect show if wrong user" do
    flunk "test is not yet written"
  end

  test "Should redirect index if wrong user" do
    flunk "test is not yet written"
  end

  test "Should redirect edit if wrong user" do
    flunk "test is not yet written"
  end

  test "Should redirect update if wrong user" do
    flunk "test is not yet written"
  end
  #--------------------------------------------------

  # --tests for redirecting if not logged in ----------
  test "Should redirect create if not signed in" do
    flunk "test is not yet written"
  end

  test "Should redirect new if not signed in" do
    flunk "test is not yet written"
  end

  test "Should redirect destroy if not signed in" do
    flunk "test is not yet written"
  end

  test "Should redirect show if not signed in" do
    flunk "test is not yet written"
  end

  test "Should redirect index if not signed in" do
    flunk "test is not yet written"
  end

  test "Should redirect edit if not signed in" do
    flunk "test is not yet written"
  end

  test "Should redirect update if not signed in" do
    flunk "test is not yet written"
  end
  #--------------------------------------------------
  #-----------should do actions----------------------
  test "new should render new location view" do
    flunk "test is not yet written"
  end

  test "Should create new location with correct info" do
    flunk "test is not yet written"
  end

  test "should not create new location with incorrect info" do
    flunk "test is not yet written"
  end

  test "new should render edit_location view" do
    flunk "test is not yet written"
  end

  test "Should update location with correct info" do
    flunk "test is not yet written"
  end

  test "Should not update location with incorrect info" do
    flunk "test is not yet written"
  end

  test "Should destroy location" do
    flunk "test is not yet written"
  end

  test "index should show visible locations" do
    flunk "test is not yet written"
  end

  test "show should show location" do
    flunk "test is not yet written"
  end
end